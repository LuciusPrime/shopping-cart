
## Availible Scripts

### api/
install packages
`npm install`

start
`nnpm run start`

### client/
install packages
`npm install`

build dist files
`npm run build`

start vue client
`npm run serve`

run dev
`npm run start`
